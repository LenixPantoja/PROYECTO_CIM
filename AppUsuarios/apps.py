""" Librerias """
from django.apps import AppConfig

# Definir la configuración de la aplicación de AppUsuarios
class AppusuariosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'AppUsuarios'
